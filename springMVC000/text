1：业务相关的异常用controller方法里方法加异常
2：异常分两部分局部和全局
3：JSON源码
4:ajax
5:中文乱码
6：小数默认是双精度
7：常见的数据结构有：
    数组（长度固定，类型相同）
    列表（大小可调，能重复 arraylist除了插入删除比treelist其他都挺快treelist（频繁的时候用）采用
        是有序线性表,二分查找,不可能比较n次啊,比较n次你等于是把整个线性表遍历了一遍.二
        分查找每次可以排除一半元素.比如123456789,你要找2,首先查中间元素5,大于2,所以直
        接排除掉5右边的6789然后在1234里继续二分查找.每次排除1/2的元素,所以是O(log2n)）
    集合（元素不重复）
    堆栈（先进后出）
    队列（先进先出）
    链表（多个子节点构成）
    hashMap数据结构（键值对映射）
    树（节点组成的数据结构，只有一个父节点）
    图（由节点和边构成）

